---
title: "Experience"
draft: false
---

### HighQube AI *(Stealth-mode Startup)* — Remote, USA
**AI Engineer** | Dec 2025 – Present

- **Architected a multi-stage LLM orchestration pipeline** using **Gemini 2.5 Flash** and **MarkItDown** to automate extraction of complex ocean freight rates from unstructured 250k+ token Excel and PDF contracts, achieving **98%+ extraction F1 score**.
- **Engineered a "Global Router–Local Scout" hierarchical pattern** to bypass LLM output constraints; utilized heuristic density detection to pre-emptively split high-complexity tables, reducing end-to-end processing time by **65%** and achieving **100% data coverage**.
- **Developed a self-healing asynchronous ingestion engine** using **asyncio** and **Semaphores**; optimized throughput by **500%** while managing strict TPM/RPM quotas and implementing automated recursive chunk-splitting for high-density datasets.
- **Implemented Instruction-Augmented Context Caching** to maintain positional persistence across massive documents; reduced API operational costs by **90%** and minimized TTFT latency by embedding domain-specific logic and navigation rules into the cache layer.
- **Optimized data integrity** with a stateless, idempotent pipeline using **UUID v5 deterministic hashing** and **SHA-256 key hashing**; ensured **100% referential integrity** and **98%+ extraction accuracy** across parallelized batch processing calls.
- **Constructed a graph-based rate consolidation engine** to dynamically link "Outport/Inport Arbitraries" with core ocean freight routes, providing unified total-rate calculations for downstream logistics management systems.
- **Built a production-grade telemetry and validation framework** using **tqdm** and synchronized logging to track real-time progress and granular token usage; utilized "LLM-as-a-Judge" metrics (Precision, Recall, F1) to ensure system reliability.
- **Architected a multi-turn autonomous agentic AI framework** using **LangGraph**; engineered an iterative "Plan–Retrieve–Fill–Repair" loop that dynamically maps carrier data to 200+ arbitrary customer template layouts.
- **Led Red Teaming and adversarial robustness efforts**; developed a custom Markdown Sanitizer and integrated **NVIDIA NeMo Guardrails** as a security boundary to enforce execution sandboxing and prevent prompt injection attacks.
- **Engineered a deterministic tool-access layer** via **Model Context Protocol (MCP)**, enabling external AI agents to perform high-fidelity rate lookups while maintaining tenant isolation and referential integrity.

---

### Coal India Limited
**Engineering Manager** | 5 years

- Led **80+ engineers** and managed maintenance operations for **1,000+ mining machines** across 80,000 sq. km.
- Built a **predictive maintenance system** using regression trees, reducing unplanned downtime.
- Designed a **spares optimization model** that saved **$50,000+** annually by reducing excess inventory.
- Drove uptime improvements of **16.4%** through data-driven maintenance scheduling and cross-team execution.
